{% capture homepageLink %}
    {{ shop.url }}
{% endcapture %}

<div>
    <h2 class="test">{{ 'general.404.title' | t }}</h2>
    <p>{{ 'general.404.subtext_html' | t }}</p>
    <a href="{{ homepageLink }} ">return to shop</a> 
</div>
